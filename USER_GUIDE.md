# 桌面机器人 - 完整使用指南

## 📖 目录

1. [快速开始](#快速开始)
2. [键盘宏功能](#键盘宏功能)
3. [快速命令面板](#快速命令面板)
4. [执行历史与统计](#执行历史与统计)
5. [快捷键大全](#快捷键大全)
6. [常见问题](#常见问题)

---

## 快速开始

### 安装与启动
```bash
# 安装依赖
npm install

# 启动应用
npm start

# 开发模式（打开 DevTools）
npm run dev
```

### 界面概览

应用界面分为 7 个主要面板：
- **指令** - 快捷指令和键盘宏
- **工作流** - 可视化自动化流程
- **模板库** - 预设模板
- **高级** - 剪贴板、截图、连点器等
- **脚本** - 脚本管理
- **录制** - 录制操作
- **定时** - 定时任务
- **设置** - 应用设置

---

## 键盘宏功能

### 什么是键盘宏？

键盘宏允许你将一系列操作绑定到单个功能键（F1-F12）上，一键执行复杂操作。

### 创建宏

1. 打开「指令」面板
2. 滚动到「键盘宏」区域
3. 点击「➕ 新建宏」
4. 输入宏名称（如：复制粘贴）
5. 选择快捷键（F1-F12）

### 添加宏命令

1. 点击「➕ 添加宏命令」
2. 填写以下信息：
   - **按键/快捷键**：触发此命令的按键
   - **执行动作**：选择动作类型
   - **值**：动作的具体内容
   - **延迟**：执行后等待时间（秒）

### 动作类型说明

| 类型 | 说明 | 示例 |
|------|------|------|
| 发送快捷键 | 模拟键盘快捷键 | `Ctrl+C`, `Alt+Tab` |
| 输入文本 | 输入指定文本 | `你好，世界！` |
| 自定义指令 | 执行自定义命令 | `截图`, `复制` |

### 快捷键格式

支持以下格式：
- **单键**：`A`, `F1`, `Enter`, `Space`
- **组合键**：
  - `Ctrl+Alt+K`
  - `Cmd+Shift+S`
  - `Ctrl+Shift+Alt+Delete`

支持的修饰键：
- `Ctrl` 或 `Control`
- `Cmd` 或 `Command`
- `Alt` 或 `Option`
- `Shift`

### 预设宏模板

应用内置了 5 个常用宏模板：

| 名称 | 快捷键 | 功能描述 |
|------|--------|----------|
| 复制粘贴 | F1 | 复制→切换应用→粘贴 |
| 保存全部 | F2 | 保存并最小化窗口 |
| 打开开发环境 | F3 | 打开浏览器访问本地开发服务器 |
| 问候语 | F4 | 快速输入常用问候语 |
| 关闭所有窗口 | F5 | 关闭当前应用的所有窗口 |

### 宏管理

- **编辑**：点击宏列表中的宏进行编辑
- **保存**：点击「💾 保存」保存更改
- **删除**：点击宏右侧的 🗑️ 按钮
- **导出**：点击 💾 按钮导出单个宏
- **导入**：点击「📂 导入」导入宏文件
- **导出全部**：点击「💾 导出全部」备份所有宏

### 使用宏

有三种方式使用宏：

1. **快捷键触发**：按设置的 F1-F12 键
2. **点击运行**：在宏编辑器中点击「▶️ 运行」
3. **快速命令面板**：`Ctrl+K` 搜索执行

---

## 快速命令面板

### 打开方式
- 快捷键：`Ctrl+K` (Windows/Linux) 或 `Cmd+K` (macOS)

### 功能说明

快速命令面板类似 Spotlight，可快速搜索和执行：

- 所有已保存的宏
- 所有已保存的脚本
- 常用快捷动作

### 操作方式

| 按键 | 功能 |
|------|------|
| `↑` | 上一个选项 |
| `↓` | 下一个选项 |
| `Enter` | 执行选中项 |
| `Escape` | 关闭面板 |

### 搜索关键词

- 宏命令：`宏`, `fast`, `keyboard`
- 脚本：`脚本`, `script`
- 截图：`截图`, `屏幕`, `screenshot`
- 剪贴板：`剪贴板`, `clipboard`
- 鼠标：`鼠标`, `mouse`
- 浏览器：`浏览器`, `chrome`

---

## 执行历史与统计

### 查看执行历史

1. 打开「设置」面板
2. 滚动到「📜 执行历史」区域
3. 查看最近 100 条执行记录

### 查看执行统计

1. 打开「设置」面板
2. 滚动到「📊 执行统计」区域
3. 查看：
   - 总执行次数
   - 成功次数
   - 失败次数
   - 成功率

### 管理数据

- **清空历史**：点击「清空历史」按钮
- **重置统计**：点击「重置统计」按钮

---

## 快捷键大全

### 全局快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+K` / `Cmd+K` | 打开快速命令面板 |
| `Escape` | 关闭快速命令面板 |
| `F6` | 运行当前脚本 |
| `F7` | 开始/停止录制 |
| `F1-F12` | 执行绑定的宏 |

### 快速命令面板导航

| 按键 | 功能 |
|------|------|
| `↑` | 上一个选项 |
| `↓` | 下一个选项 |
| `Enter` | 执行选中项 |
| `Escape` | 关闭面板 |

---

## 常见问题

### Q: 宏不执行怎么办？
A: 检查以下几点：
1. 确保宏已保存
2. 确保快捷键设置正确
3. 确保应用窗口未最小化
4. macOS 用户请检查辅助功能权限

### Q: 如何备份我的宏和脚本？
A: 有两种方式：
1. 使用「设置」→「导出数据」备份全部数据
2. 单独导出宏：宏列表 → 点击 💾 按钮

### Q: 快速命令面板搜索不到我的宏？
A: 请确保：
1. 宏已经保存
2. 尝试使用关键词「宏」或宏名称

### Q: 执行历史最多保留多少条？
A: 最多保留 100 条记录，超出会自动删除最早的记录

### Q: 如何删除一个宏？
A: 在宏列表中找到目标宏，点击右侧的 🗑️ 按钮

### Q: 支持哪些快捷键格式？
A: 支持单键（A-Z, F1-F12）和组合键（Ctrl+X, Cmd+Shift+S 等）

---

## 更多资源

- [README.md](README.md) - 项目说明
- [FEATURES.md](FEATURES.md) - 功能详情
- [CHANGELOG.md](CHANGELOG.md) - 更新日志
- [AI_ASSISTANT.md](AI_ASSISTANT.md) - AI 助手说明
